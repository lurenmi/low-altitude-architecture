# 低空数字孪生平台本地部署说明

## 1. 推荐启动方式（避免 PowerShell 执行策略问题）

直接双击或命令行运行：

```cmd
启动本地预览.cmd
```

默认端口为 `8090`，也可以指定端口：

```cmd
启动本地预览.cmd 8092
```

如需静态预览（不启后端）：

```cmd
启动本地预览.cmd -Static
```

## 2. 兼容启动方式（PowerShell）

```powershell
powershell -ExecutionPolicy Bypass -File .\la_platform\启动平台.ps1
```

启动后访问：

```text
http://localhost:8090/
```

该模式会自动：

1. 初始化 SQLite 业务数据库；
2. 启动后端 API；
3. 提供前端页面（低空一张图、飞行服务、空域设施）。

## 3. 一键自查（接口 + 页面结构）

```cmd
self_check.cmd
```

支持自定义端口：

```cmd
self_check.cmd -Port 8096
```

自查覆盖：
- 服务健康检查；
- 三大板块核心接口（低空一张图、飞行服务、空域设施）；
- 审批规则校验、任务回放加载与重建；
- 页面关键挂载点与前端逻辑钩子（地图、3D 渲染、回放控件）。

## 4. 本次已补齐的生产化交互

- 审批规则可配置：可在“飞行服务”页面修改风险阈值并保存到数据库。
- 地图编辑：可在“低空一张图”页面对对象执行新建、更新、删除，并记录操作留痕。
- 任务回放：可按飞行计划加载轨迹，支持播放/暂停/倍速/滑块定位及轨迹重建。

## 5. 关键文件

- `la_platform/backend/schema.sql`
- `la_platform/backend/init_db.py`
- `la_platform/backend/api_server.py`
- `la_platform/frontend/index.html`
- `la_platform/frontend/styles.css`
- `la_platform/frontend/app.js`
- `self_check.ps1`
- `self_check.cmd`
- `启动本地预览.cmd`

## 6. 常见问题

- 页面打不开：确认脚本启动窗口没有报错，且端口 `8090` 未被占用。
- 数据异常：重新执行 `la_platform/backend/init_db.py` 初始化数据库。
- `ps1` 运行被拦截：优先使用 `启动本地预览.cmd` 或 `self_check.cmd`。
